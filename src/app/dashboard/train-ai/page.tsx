import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Train AI | Klever",
  description: "Train your AI Agent with documents, URLs, and audio files to generate intelligent content.",
};

export default function TrainAIPage() {
  return null;
}
